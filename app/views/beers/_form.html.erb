<%= simple_form_for beer do |f| %>
  <%= f.input :name %>
  <%= f.input :brewery %>
  <%= f.input :style, collection: Beer.styles %>
  <%= f.input :country, priority: [ "France" ] %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
